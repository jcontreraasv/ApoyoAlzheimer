<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jaime.contreras.apoyoalzheimer">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme1">

        <activity android:name=".funciones.ConsejosAlzheimerActivity" />
        <activity android:name=".funciones.ConsejosUsuarioActivity" />
        <activity android:name=".juegos.JuegoOperacionesActivity" />
        <activity android:name=".juegos.JuegoExamenFinalActivity" />
        <activity android:name=".juegos.JuegoColoresActivity" />
        <activity android:name=".juegos.JuegoSecuenciaActivity" />
        <activity android:name=".juegos.JuegoExamenActivity" />
        <activity android:name=".inicio.OlvidarContraActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC1OZJigXV4cXyP_VqTuR4k0F-ztuc5Nv0" />

        <activity
            android:name=".funciones.MiUbicacionActivity"
            android:label="@string/title_activity_mi_ubicacion" />
        <activity android:name=".funciones.AjustesActivity" />
        <activity android:name=".juegos.MenuJuegosActivity" />
        <activity android:name=".inicio.RegistroActivity" />
        <activity android:name=".funciones.MainMenuActivity" />
        <activity
            android:name=".inicio.SplashscreenActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".inicio.MainActivity" />
        <activity android:name=".inicio.BienvenidoActivity" />
    </application>

</manifest>